#!/usr/bin/env bash
set -euo pipefail

# Template author: add your quality checks here (linting, formatting, tests, etc.)
# Example: make check
# Example: cd src && go vet ./...

# Beads will append its integration block below when you run 'bd init'.
